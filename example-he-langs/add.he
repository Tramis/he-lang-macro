one! = {
    () => ;
}

add! = {
    ($a) => {
        $a | 
    };
}

print!( count!(|));
print!( count!( one!()));
// print!( count!( add!( one!())));

print!( 
    count! ( 
        add! ( 
            add! ( 
                one!()
            )
        )
    )
);